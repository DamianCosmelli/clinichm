/**
 * validacion de schemas de formularios
 * 
 * 
 * 
 import * as z from "zod";
 
  const schema = z.object({
  name: z.string().min(1, "El nombre es obligatorio"),
  email: z.string().email("Correo inválido"),
});

 */