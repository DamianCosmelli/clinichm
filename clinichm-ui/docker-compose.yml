services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
      - TZ=America/Argentina/Buenos_Aires  # Zona horaria de Argentina
    env_file:
      - .env